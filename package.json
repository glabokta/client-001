{
    "scripts": {
      "export:dev": "a0deploy export --strip --format yaml --output_folder . --config_file ./config.json",
      "import:dev": "a0deploy import --input_file tenant.yaml --config_file ./config.json"
    }
}